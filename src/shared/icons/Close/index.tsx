import { FC } from 'react';

import { SClose } from './styles';
import { SIconWrapper } from '../styles';

export const CloseIcon: FC = () => {
  return (
    <SIconWrapper>
      <SClose />
    </SIconWrapper>
  );
};
