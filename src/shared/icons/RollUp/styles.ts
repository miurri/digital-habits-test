import styled from 'styled-components';

import { Colors } from '../../../styles/shared/constants';

export const SRollUp = styled.div`
  width: 10px;
  height: 1px;

  background-color: ${Colors.BLACK};
`;
